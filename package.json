{
  "name": "dmais_maritaria-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && tsc",
    "start": "npm run build && node backend/dist/server.js",
    "rodar": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}